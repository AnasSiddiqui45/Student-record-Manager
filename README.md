# Student-record-Manager
A Python-based Student Marks Management System that allows users to insert, update, delete, and view student records using lists and functions. It provides simple CRUD operations through user input without using any database, focusing on core Python logic and control flow.
